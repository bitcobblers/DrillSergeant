<Project>
    <Target Name="UpdateNuSpecProperties" BeforeTargets="GenerateNuspec">
        <PropertyGroup>
            <NuspecProperties>Configuration=Debug;PackageVersion=$(PackageVersion)</NuspecProperties>
        </PropertyGroup>
    </Target>
</Project>