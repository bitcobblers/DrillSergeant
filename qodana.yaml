version: "1.0"
linter: jetbrains/qodana-dotnet:2023.2
dotnet:
  solution: DrillSergeant.sln
fixesStrategy: apply
profile:
  name: qodana.recommended
exclude:
  - name: All
    paths:
      - src
      - test
